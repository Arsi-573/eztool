<!DOCTYPE html>
<html lang="fi">
<head>
    <meta charset="UTF-8">
    <title>{{ company_name }}</title>
    <style>
        body { font-family: sans-serif; bacround-color: #f0f2f5; text-align: center; padding-top: 50px; }
        .card { bacround: white; border-radius: 15px; box-shadow: 0 4px 15px rgba(0,0,0,0.1); display: inline-block; padding: 40px; max-width: 500px; }
        h1 { color: #1a73e8; margin-bottom: 10px; }
        .domain { color: #5f6368; font-weight: bold; }
    </style>
</head>
<body>
    <div class="card">
        <h1>{{ company_name }}</h1>
        <p>Yrityksen hallintaportaali on valmiina käyttöön.</p>
        <p class="domain">Domain: {{ domain_name }}</p>
        <hr>
        <p><small>Tämä sivu on automaattisesti generoitu Ansiblella.</small></p>
    </div>
</body>
</html>    
